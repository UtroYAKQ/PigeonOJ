# ============ Web 框架 ============
# 版本策略：钉住小版本（与生产实测一致）；升级需在测试环境验证后手动提升
fastapi==0.141.1
uvicorn[standard]==0.52.4

# ============ 配置 / 校验 ============
pydantic==2.13.4
pydantic-settings==2.15.0

# ============ 数据库（SQLAlchemy 异步 + 迁移）============
sqlalchemy[asyncio]==2.0.52
alembic==1.19.1
asyncpg==0.31.0

# ============ 缓存 ============
redis==8.1.0

# ============ 判题节点网关（gRPC）============
grpcio==1.83.0
protobuf==7.36.0       # 生成的 judge_pb2*.py 依赖 google.protobuf

# ============ 安全 ============
bcrypt==5.0.0          # 密码哈希

# ============ IP 地理位置（离线 xdb，省市级；数据文件 app/data/ip2region.xdb 随仓库分发）============
py-ip2region==3.0.4

# ============ 对象存储 ============
minio==7.2.20          # MinIO SDK（生产部署）
python-multipart==0.0.32   # FastAPI multipart/form-data 上传

# ============ 测试（开发依赖）============
# 运行 pytest 前安装：pip install -r requirements-dev.txt
