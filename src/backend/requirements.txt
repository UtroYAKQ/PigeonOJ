# ============ Web 框架 ============
fastapi
uvicorn[standard]

# ============ 配置 / 校验 ============
pydantic
pydantic-settings

# ============ 数据库（SQLAlchemy 异步 + 迁移）============
sqlalchemy[asyncio]
alembic
asyncpg

# ============ 缓存 / 异步任务 ============
redis
celery

# ============ AI 能力（业务阶段按需安装，见 docs/contracts/ai.md）============
# langgraph
# litellm

# ============ 测试（开发依赖）============
# 运行 pytest 前安装：pip install -r requirements-dev.txt
