# ============ Web 框架 ============
fastapi
uvicorn[standard]

# ============ 配置 / 校验 ============
pydantic
pydantic-settings

# ============ 数据库（SQLAlchemy 异步 + 迁移）============
sqlalchemy[asyncio]
alembic
asyncpg

# ============ 缓存 ============
redis

# ============ 判题节点网关（gRPC）============
grpcio

# ============ 安全 ============
bcrypt                # 密码哈希

# ============ 对象存储 ============
minio                  # MinIO SDK（生产部署）
python-multipart       # FastAPI multipart/form-data 上传

# ============ 测试（开发依赖）============
# 运行 pytest 前安装：pip install -r requirements-dev.txt
