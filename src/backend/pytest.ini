[pytest]
asyncio_mode = auto
# 全部 async 测试 / fixture 共享会话级事件循环：
# 模块级异步引擎（app.shared.database）的连接池只绑定一个循环，避免跨循环复用错误
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
testpaths = tests
